{
  "name": "cap-xml",
  "version": "0.0.1",
  "description": "Serverless (AWS Lambda) project for CAP XML service",
  "main": "handler.js",
  "scripts": {
    "lint": "standard",
    "unit-test": "lab test -v -c --coverage-path lib -r console -o stdout -r html -o test/output.html -m 5000",
    "test": "npm run lint && npm run unit-test"
  },
  "author": "The Environment Agency",
  "license": "OGL",
  "dependencies": {
    "aws-sdk": "^2.95.0",
    "xml2js": "^0.4.17"
  },
  "devDependencies": {
    "code": "^4.1.0",
    "lab": "^14.1.1"
  }
}
