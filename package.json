{
  "name": "cap-xml",
  "version": "0.0.2",
  "description": "Serverless (AWS Lambda) project for CAP XML service",
  "engines": {
    "node": ">8"
  },
  "main": "handler.js",
  "scripts": {
    "deploy": "npm run validate-config && sls deploy -v --force",
    "lint": "standard",
    "unit-test": "lab test -v -c --coverage-exclude lib/helpers -r console -o stdout -r html -o test/output.html -m 5000",
    "test": "npm run lint && npm run unit-test",
    "validate-config": "node ./config/index.js"
  },
  "author": "The Environment Agency",
  "license": "OGL",
  "dependencies": {
    "feed": "1.1.1",
    "joi": "13.3.0",
    "moment": "2.22.1",
    "pg": "7.4.3",
    "sql": "0.78.0",
    "xml2js": "0.4.19"
  },
  "devDependencies": {
    "code": "5.2.0",
    "lab": "15.4.5",
    "serverless": "1.27.2",
    "standard": "11.0.1"
  }
}
